# 顔検出テスト手順

## 🎯 目的
顔検出機能が正しく動作するかを確認するため、サンプル画像を使ってテストを実行します。

## 📋 テスト環境

### 起動済みのサービス
- ローカルHTTPサーバー: `http://localhost:8080`
- テストページ: `http://localhost:8080/test-face-detection.html`
- メインアプリ: `http://localhost:8080/index.html`

## 🧪 テスト手順

### 1. テストページにアクセス

ブラウザで以下のURLを開いてください：
```
http://localhost:8080/test-face-detection.html
```

### 2. モデル読み込みの確認

ページを開くと、自動的にTensorFlow.jsと顔検出モデルが読み込まれます。
- ステータス表示が「✅ 準備完了！」になるまで待ちます
- 通常、初回は10-30秒程度かかります
- 詳細ログで読み込み状況を確認できます

### 3. サンプル画像で顔検出をテスト

#### 方法A: デフォルト画像を使用
1. 「画像を読み込む」ボタンをクリック
   - デフォルトでサンプル画像のURLが入力されています
   - URL: `https://www.biteki.com/wp-content/uploads/2023/10/230724_0258-1.jpg`

2. 画像が表示されたら「🧪 顔検出を開始」ボタンをクリック

3. 結果を確認：
   - ✅ **成功の場合**: 画像上に緑色のバウンディングボックスとキーポイントが表示されます
   - ❌ **失敗の場合**: ステータスに「顔が検出されませんでした」と表示されます
   - 詳細ログで検出の詳細情報を確認できます

#### 方法B: 別の画像を使用
1. 画像URLのテキストボックスに任意の画像URLを入力
2. 「画像を読み込む」ボタンをクリック
3. 「🧪 顔検出を開始」ボタンをクリック

### 4. デバッグ情報の確認

テストページには詳細なログが表示されます：
- TensorFlow.jsのバージョンとバックエンド情報
- モデルの読み込み時間
- 検出された顔の数
- 各顔のプロパティ（box, boundingBox, keypoints）
- 検出処理時間

## 📊 期待される結果

### 成功の場合
- ステータス: 「✅ 成功！X個の顔を検出しました」
- 画像上に表示される要素:
  - 緑色のバウンディングボックス（四角形）
  - 「顔 #1」のラベル
  - 顔のキーポイント（青緑色の小さな点、約468個）
  - 主要な顔のパーツ（目、眉、鼻、唇、輪郭）が色分けされた線で描画

### ログの例（成功時）
```
[時刻] ✅ モデル読み込み成功 (2.5秒)
[時刻] 📝 画像サイズ: 1200 x 1600
[時刻] 📝 検出処理時間: 234ms
[時刻] 📝 検出された顔の数: 1
[時刻] ✅ 顔検出成功！1個の顔を検出
[時刻] 📝 プロパティ: box, keypoints, ...
[時刻] 📝 キーポイント数: 468
[時刻] ✅ キーポイント描画完了
[時刻] ✅ 顔の描画完了
```

## 🔧 トラブルシューティング

### モデルが読み込めない
- インターネット接続を確認
- ブラウザのコンソールでエラーを確認
- ページをリロード（Ctrl+R または Cmd+R）

### 画像が読み込めない
- CORS制限の可能性があります
- ブラウザのコンソールでCORSエラーを確認
- 別の画像URLを試す
- ローカル画像を使用する場合は、同じディレクトリに配置

### 顔が検出されない
- 画像に明確な顔が写っているか確認
- 顔が小さすぎたり、横を向いていたりしないか確認
- 画像の品質を確認（解像度、明るさなど）
- 別の画像で試す

## 🎥 メインアプリのテスト

テストページで顔検出が成功したら、メインアプリでカメラを使ったリアルタイム検出をテストします：

1. `http://localhost:8080/index.html` にアクセス
2. 「カメラを起動」ボタンをクリック
3. カメラアクセスを許可
4. 「体検出 ⇄ 顔検出」ボタンをクリックして顔検出モードに切り替え
5. 画面左上のデバッグ情報を確認：
   - 「✅ 顔検出: X個」と表示されれば成功
   - 「❌ 顔が検出されていません」と表示される場合:
     - 明るい場所で試す
     - 顔を正面に向ける
     - カメラに近づく/遠ざかる

## 📝 改善点と注意事項

### 実装の改善点
1. **モデル設定の最適化**
   - `refineLandmarks: true` に設定してより高精度な検出を実現
   - `maxFaces: 5` で複数の顔を検出可能

2. **エラーハンドリングの強化**
   - 詳細なデバッグログを追加
   - エラー時のスタックトレースを出力

3. **描画ロジックの改善**
   - バウンディングボックスがない場合、キーポイントから計算
   - 複数の顔を正しく描画
   - より見やすいラベル表示

### 既知の問題
- 外部画像URLを使用する場合、CORS制限により読み込めないことがあります
- Node.js環境でのテストは、画像形式の互換性の問題で動作しません（ブラウザ環境のみ）

## ✅ 次のステップ

テストが成功したら：
1. ブラウザのコンソールログを確認
2. 問題があれば、ログの詳細を確認
3. 必要に応じてコードを調整

---

**作成日**: 2025-11-12  
**バージョン**: 1.2.2-face-detection-fix
